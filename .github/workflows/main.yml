on:
  push:
    branches:
      - jana




jobs:
  build:
    runs-on: ubuntu-latest

    steps:
  
      - uses: actions/checkout@v3
      - name: "login to docker hub"
        run: docker login --username basant9 --password dckr_pat_1nUCXOT2EepxJH-zgETIPCrrdgA